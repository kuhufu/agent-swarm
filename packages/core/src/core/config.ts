import type { AgentSwarmRootConfig } from "./types.js";

/**
 * Helper to define agent-swarm config with type checking.
 */
export function defineConfig(config: AgentSwarmRootConfig): AgentSwarmRootConfig {
  return config;
}
